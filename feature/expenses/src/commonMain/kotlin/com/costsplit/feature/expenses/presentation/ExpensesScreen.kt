package com.costsplit.feature.expenses.presentation

import androidx.compose.runtime.Composable

@Composable
fun ExpensesScreen(viewModel: ExpensesViewModel) {
    // Intentionally empty. UI will collect state/effects and send ExpensesIntent values.
}
